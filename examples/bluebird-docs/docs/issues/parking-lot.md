# 보류함

아직 굳히지 않은 이야기를 적어 둔다. `issues/**` 는 config 의 `docs.exclude` 에 들어 있어
검사 대상이 아니다 — 여기 적힌 수는 정본과 어긋나도 게이트가 보지 않는다.

- 1차 전환 대상을 12개로 줄이자는 의견이 있다.
- 슬롯을 여덟 자리로 늘릴지 논의 중이다.
- 공유 패키지를 둘로 쪼개는 안은 아직 결정 기록에 올리지 않았다.
