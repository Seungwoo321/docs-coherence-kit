# 필드 규격

## §4 조각 필드

### §4.2 배치

`kind` 는 배치 판정의 입력이 아니다. 배치를 가르는 물음은 도메인을 아는가 하나다
([결정 기록](#doc/decisions) ADR-004). 내용은 그대로 반영됐는데 근거 번호가 없다.

## §5 현황

파이프라인 현황은 콘솔 출력으로 확인한다(ADR-003).

## §6 예시

인용처럼 보이지만 예시일 뿐인 번호는 코드 펜스 안에 둔다.

```json
{ "decision": "ADR-001" }
```
