# 단계 메모

## §2 층 구조 요약

층 구조의 아래층은 호스트다. 그 위에 기능블록 패키지가 얹힌다.

## §3 패키지 이름 이력

아래는 전환 전 문서에서 옮겨 온 원문이라 손대지 않는다.

```md
`booking-authoring` 은 옛 이름이다.
`booking-authoring` 과 `fence-only-name` 은 더 쓰지 않는다.
```

## §4 현행

현행 기능블록 패키지는 `booking-core` 다.
