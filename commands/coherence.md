---
description: 문서 집합의 정합성을 검사한다. 결정적 축을 먼저 돌려 게이트를 세우고, 통과하면 판정 축을 격리 실행한 뒤, 결과를 전수 대조해 리포트를 낸다.
argument-hint: "[--axes <축1,축2>] [--run-id <id>] [--waive <코드>]"
---

# /coherence

`dck-coherence` 스킬을 그대로 따른다 — 순서·게이트·보고 형식 전부 그 스킬이 소유하고, 이 커맨드는 진입점일 뿐이다.

인자: `$ARGUMENTS`
